#ifndef PLAYER_H
#define PLAYER_H

#include <string>
#include <SFML/Graphics.hpp>
#include "constants.h"
#include "Ball.h"

class Player
{
public:
  Player(std::string &);

  bool takeTurn();
  void shootBall();

  int playerID{};
  bool turn{};
  void setEnemy();
  
private:
  Col assignBalls{};
  std::string playerName;
  //Player & enemy{};
  //Ball* currBall;
};


#endif
